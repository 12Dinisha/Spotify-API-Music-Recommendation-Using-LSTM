name: Spotify API Music Recommendation - CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@The failure occurred during the preparation of metadatav2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version with `pyproject.toml`, leading to a `subprocess-ex: '3.x'
    - name: Upgrade pip and setuptools
      run: |
        python -m pip install --upgrade pip setuptools
    - name: Install dependencies
