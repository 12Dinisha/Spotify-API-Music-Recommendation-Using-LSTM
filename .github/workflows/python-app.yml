name: Spotify API Music Recommendation - CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-l compatible versions

    steps:
    - uses: actions/checkout@v2 and platform.


    - name: Set up Python
      uses: actions/setup-python modify your `.github/workflows/python-app.yml` to include these steps:

```yaml
name: Spotify API Music@v2
      with:
        python-version: 3.8  Recommendation - CI

on: [push, pull_request]

 # or any other compatible version

    - name: Upgrade pipjobs:
  build:

    runs-on: ubuntu-latest

    steps:

      run: python -m pip install --upgrade pip

    - name: Install dependencies
      run: pip install pandas    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
==1.3.3
        pytest
